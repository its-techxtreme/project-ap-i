# Phase 7 will populate the real Dockerfile.
# This is a structural placeholder so infra references compile.
FROM node:20-slim
WORKDIR /app

# Placeholder — real image built in Phase 7
RUN echo "Worker placeholder — Phase 7 will populate this"
